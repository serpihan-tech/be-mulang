import { HttpContext } from '@adonisjs/core/http'
import { DateTime } from 'luxon'
import db from '@adonisjs/lucid/services/db'

export default class TeacherDashboardService {
  async dashboardTeacher(ctx: HttpContext) {
    //
  }
}
